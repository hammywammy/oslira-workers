{
  "name": "oslira-workers",
  "version": "1.0.0",
  "description": "Oslira Backend - Cloudflare Workers API",
  "type": "module",
  "main": "src/index.ts",
"scripts": {
  "dev": "wrangler dev --env staging",
  "dev:prod": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "tail": "wrangler tail",
    "tail:staging": "wrangler tail --env staging",
    "secret:put": "wrangler secret put",
    "secret:list": "wrangler secret list",
    "build": "node build-with-aws-config.js",
    "deploy:secrets": "bash deploy-aws-secrets.sh"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "wrangler": "^3.80.0",
    "typescript": "^5.3.3"
  },
  "keywords": [
    "cloudflare-workers",
    "api",
    "backend",
    "ai",
    "lead-generation"
  ],
  "author": "Hamza Williams",
  "license": "MIT"
}
